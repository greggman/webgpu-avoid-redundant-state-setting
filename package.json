{
  "name": "webgpu-avoid-redundant-state-setting",
  "version": "0.0.1",
  "description": "check for and/or avoid redundant state setting in WebGPU",
  "main": "webgpu-avoid-redundant-state-setting.js",
  "module": "webgpu-avoid-redundant-state-setting.js",
  "type": "module",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "check": "npm run lint",
    "check-ci": "npm run pre-push",
    "lint": "eslint \"src/**/*.{js}\"",
    "pre-push": "npm run lint && npm run test",
    "test": "echo \"need to wait for puppeteer to support WebGPU or make a mock WebGPU impl\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greggman/webgpu-avoid-redundant-state-setting.git"
  },
  "keywords": [
    "webgpu"
  ],
  "author": "Gregg Tavares",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/greggman/webgpu-avoid-redundant-state-setting/issues"
  },
  "homepage": "https://github.com/greggman/webgpu-avoid-redundant-state-setting#readme",
  "devDependencies": {
    "eslint": "^8.40.0",
    "eslint-plugin-html": "^7.1.0",
    "eslint-plugin-one-variable-per-var": "^0.0.3",
    "eslint-plugin-optional-comma-spacing": "^0.0.4",
    "eslint-plugin-require-trailing-comma": "^0.0.1"
  }
}
